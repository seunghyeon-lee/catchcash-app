import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.catchcash.app",
  appName: "CatchCash",
  webDir: "out",
  server: { androidScheme: "https" },
};

export default config;
